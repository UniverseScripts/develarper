# HTTP & Async
aiohttp==3.9.5
aiofiles==23.2.1

# Schema validation
pydantic>=2.7.1

# Fireworks API (OpenAI-compatible)
openai==1.30.1

# Dev env loading
python-dotenv==1.0.1

# Retry logic
tenacity==8.3.0

# Local LLM runtime (CPU). The classifier uses this same GGUF model — no
# torch / sentence-transformers / embedding model required.
# llama-cpp-python is installed separately (Dockerfile CPU wheel) to avoid
# building from source.
